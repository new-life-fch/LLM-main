# CausalEditor 项目统一依赖文件
# 包含核心功能和RAG模块的所有依赖
# 使用命令安装: pip install -r requirements.txt

# ========================================
# 深度学习框架
# ========================================
torch==2.5.1+cu124
transformers>=4.30.0,<4.50.0  # 确保与spacy-transformers兼容
tokenizers==0.21.2
accelerate==1.9.0

# ========================================
# 向量搜索和数据处理
# ========================================
faiss-cpu==1.9.0
# faiss-gpu>=1.7.4              # GPU版本（可选，替换faiss-cpu）
numpy>=1.21.0,<2.0.0  # 兼容spaCy，避免版本冲突
pandas>=2.3.1
scipy>=1.15.3

# ========================================
# 自然语言处理
# ========================================
spacy>=3.6.0,<3.9.0  # 确保与numpy兼容的spaCy版本
# spacy-transformers>=1.3.0  # 可选：增强的transformer支持
nltk>=3.8.1  # 自然语言处理工具包
sentence-transformers>=2.2.2  # 句子嵌入模型
jellyfish>=0.11.0  # 字符串相似度计算
regex==2024.11.6

# ========================================
# 网络请求和数据获取
# ========================================
requests>=2.32.2
aiohttp>=3.8.0  # 异步HTTP客户端
async-timeout>=4.0.3  # 异步超时控制
wikipedia>=1.4.0  # Wikipedia API
# wikidata>=0.7.0  # Wikidata API（可选）
beautifulsoup4==4.12.3

# ========================================
# 数据格式和序列化
# ========================================
datasets==3.6.0
pyyaml>=6.0.2
jsonlines>=3.0.0  # JSON Lines格式支持
safetensors==0.5.3

# ========================================
# 进度显示和日志
# ========================================
tqdm>=4.66.3
# loguru>=0.6.0  # 日志记录（可选）

# ========================================
# 机器学习和科学计算
# ========================================
scikit-learn==1.7.1
matplotlib==3.9.2

# ========================================
# 量化和优化支持
# ========================================
bitsandbytes==0.46.1

# ========================================
# 系统和工具依赖
# ========================================
huggingface_hub==0.33.4
psutil==6.1.0
filelock==3.16.1
fsspec==2024.10.0
packaging==25.0
sympy==1.13.1

# ========================================
# 开发和测试工具（可选）
# ========================================
# pytest>=7.0.0  # 测试框架
# pytest-cov>=4.0.0  # 测试覆盖率
# black>=22.0.0  # 代码格式化
# flake8>=5.0.0  # 代码检查

# ========================================
# 可选的高级功能依赖
# ========================================
# elasticsearch>=8.0.0  # Elasticsearch支持
# chromadb>=0.4.0  # ChromaDB向量数据库
# pinecone-client>=2.0.0  # Pinecone向量数据库
# intel-extension-for-pytorch  # Intel CPU优化
# onnxruntime>=1.12.0  # ONNX运行时

# ========================================
# 安装说明
# ========================================
# 1. 建议在虚拟环境中安装: python -m venv venv && source venv/bin/activate
# 2. 安装依赖: pip install -r requirements.txt
# 3. 如果使用GPU，请取消注释faiss-gpu并注释掉faiss-cpu
# 4. 开发环境可以取消注释测试和代码质量工具
# 5. 某些依赖可能需要额外的系统库支持
# 6. 版本号可根据实际需要调整，但请注意兼容性